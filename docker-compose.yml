version: '3.9'
services:
  server:
    build: ./server
    env_file: .env.example
    ports:
      - "4000:4000"
    volumes:
      - ./data:/app/data
  client:
    build: ./client
    ports:
      - "5173:4173"
    depends_on:
      - server
